import { CustomCardConfigDirective } from './custom-card-config.directive';

describe('CustomCardConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCardConfigDirective();
    expect(directive).toBeTruthy();
  });
});

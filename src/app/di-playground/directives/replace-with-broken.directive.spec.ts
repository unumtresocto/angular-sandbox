import { ReplaceWithBrokenDirective } from './replace-with-broken.directive';

describe('ReplaceWithBrokenDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceWithBrokenDirective();
    expect(directive).toBeTruthy();
  });
});

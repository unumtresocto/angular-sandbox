import { ReplaceCounterDirective } from './replace-counter.directive';

describe('ReplaceCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceCounterDirective();
    expect(directive).toBeTruthy();
  });
});

import { ReplaceWithGlobalCounterDirective } from './replace-with-global-counter.directive';

describe('ReplaceWithGlobalCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceWithGlobalCounterDirective();
    expect(directive).toBeTruthy();
  });
});

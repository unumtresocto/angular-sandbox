import { ChildSelectorDirective } from './child-selector.directive';

describe('ChildSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildSelectorDirective();
    expect(directive).toBeTruthy();
  });
});

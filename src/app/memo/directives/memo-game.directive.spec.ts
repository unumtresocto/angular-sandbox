import { MemoGameDirective } from './memo-game.directive';

describe('MemoGameDirective', () => {
  it('should create an instance', () => {
    const directive = new MemoGameDirective();
    expect(directive).toBeTruthy();
  });
});
